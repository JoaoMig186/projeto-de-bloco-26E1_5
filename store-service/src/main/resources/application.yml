spring:
  application:
    name: store-service
  profiles:
    active: dev # <-- Deixe 'dev' para rodar na IDE. Mude para 'prod' quando for subir pelo Docker Compose.

logging:
  level:
    root: INFO
    com.infnet: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE