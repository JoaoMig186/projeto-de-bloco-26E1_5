spring:
  application:
    name: review-service
  profiles:
    active: prod

api:
  endpoints:
    store: STORE-SERVICE

management:
  server:
    port: 8087
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus